import 'package:flutter/material.dart';

class DambullaMarket extends StatelessWidget {
  const DambullaMarket({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}